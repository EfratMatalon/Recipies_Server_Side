import { DateAndMorePipe } from './date-and-more.pipe';

describe('DateAndMorePipe', () => {
  it('create an instance', () => {
    const pipe = new DateAndMorePipe();
    expect(pipe).toBeTruthy();
  });
});
